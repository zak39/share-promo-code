<nav class="mb-2-5">
  <ul>
    <li><strong><a href={{ path('app_home') }} class="no-decoration blue-400">Share Promo Coade</a></strong></li>
  </ul>
  <ul>
    {% if is_granted('VOTER_VIP') %}
      <li><a href={{ path('app_vip') }} class="secondary">VIP</a></li>
    {% endif %}
    <li>
      <details class="dropdown">
        <summary>
          Compte
        </summary>
        <ul dir="rtl">
          {% if not is_granted('IS_AUTHENTICATED') %}
            <li><a href={{ path('app_login') }}>Se connecter</a></li>
          {% endif %}
          {% if is_granted('IS_AUTHENTICATED') %}
            <li><a href={{ path('app_logout') }}>Se déconnecter</a></li>
          {% endif %}
          {% if is_granted('ROLE_ADMIN') %}
            <li><a href={{ path('app_promotion_code_index') }}>Gérer les codes de promotion</a></li>
          {% endif %}
        </ul>
      </details>
    </li>
  </ul>
</nav>
